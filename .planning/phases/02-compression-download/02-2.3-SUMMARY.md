---
phase: 02-compression-download
plan: "2.3"
subsystem: api+ui
tags: [download-proxy, blob-cleanup, error-state, FileInfoCard, vercel-blob]

requires:
  - phase: 02-compression-download
    plan: "2.1"
    provides: types/upload.ts, blobService.deleteBlobSafe, /api/compress
  - phase: 02-compression-download
    plan: "2.2"
    provides: handleDownload in usePdfUpload, PLAN-2.3 comment marker in FileInfoCard

provides:
  - app/api/download/route.ts: POST proxy that streams compressed PDF and fires batch blob cleanup
  - app/api/cleanup/route.ts: extended schema with optional compressedUrl for dual-URL deletion
  - features/pdf/usePdfUpload.ts: handleReset sends compressedUrl when present
  - components/FileInfoCard.tsx: error stage renders red-bordered box with heading, message, Повторить + Загрузить другой buttons

affects: []

tech-stack:
  added: []
  patterns:
    - "head() + fetch(blobMeta.downloadUrl) streaming pattern — get() does not return a stream in @vercel/blob SDK"
    - "del([url1, url2]) batch call after new Response(stream) construction — avoids race condition"
    - "Backward-compatible Zod schema extension: optional field preserves existing callers"
    - "Spread conditional compressedUrl: ...(state.compressedBlobUrl ? { compressedUrl } : {})"

key-files:
  created:
    - app/api/download/route.ts
  modified:
    - app/api/cleanup/route.ts
    - features/pdf/usePdfUpload.ts
    - components/FileInfoCard.tsx

key-decisions:
  - "Use head() + fetch(downloadUrl) instead of get().stream — @vercel/blob get() returns metadata, not a stream"
  - "del([url1, url2]) batch call is fire-and-forget after response is constructed, not before"
  - "Sequential deleteBlobSafe calls in cleanup route — simpler error surface than Promise.all for a fire-and-forget endpoint"
  - "Backward-compatible schema: existing { url } callers unaffected, new { url, compressedUrl } path enabled"

metrics:
  duration: 15min
  completed: 2026-05-19
---

# Phase 2 Plan 2.3: Download Flow + Error Handling Summary

**Private-blob download proxy streaming compressed PDF with batch cleanup, /api/cleanup extended for dual-URL deletion, and FileInfoCard error state wired with Повторить + Загрузить другой buttons.**

## Performance

- **Duration:** ~15 min
- **Completed:** 2026-05-19
- **Tasks:** 3 completed
- **Files created:** 1
- **Files modified:** 3

## Accomplishments

- Created `/api/download` route with `runtime='nodejs'`, `maxDuration=30`, Zod schema validation, `head()` blob metadata fetch, `fetch(blobMeta.downloadUrl)` streaming response, `Content-Disposition: attachment` header (UX-03/Safari), and fire-and-forget `del([compressedBlobUrl, blobUrl])` batch cleanup after response construction (INFRA-02 completion)
- Extended `/api/cleanup` schema with optional `compressedUrl` field — backward-compatible: existing `{ url }` calls unchanged, new `{ url, compressedUrl }` path deletes both blobs sequentially
- Updated `handleReset` in `usePdfUpload` to include `compressedUrl` in the single cleanup request when `state.compressedBlobUrl` is non-null
- Replaced `{/* PLAN-2.3: add compress-error state here */}` comment in `FileInfoCard` with full error branch: red-bordered box (`#ff4d4f`), `error.heading`, `error.message`, "Повторить" (primary) and "Загрузить другой" (default) buttons; removed `void error` / `void onRetry` suppressors

## Task Commits

1. **Task 1: /api/download** — `027f4db` (feat)
2. **Task 2: /api/cleanup extension + handleReset** — `b4282d2` (feat)
3. **Task 3: FileInfoCard error state** — `b30b120` (feat)

## Files Created/Modified

- `app/api/download/route.ts` — New: POST handler with streaming, Content-Disposition attachment, batch del() cleanup
- `app/api/cleanup/route.ts` — Extended: optional compressedUrl in schema, sequential dual-delete
- `features/pdf/usePdfUpload.ts` — handleReset: spread compressedUrl when present, updated dependency array
- `components/FileInfoCard.tsx` — Error stage: replaced placeholder comment with full JSX; removed void suppressors

## Deviations from Plan

None — plan executed exactly as written. The `head()` + `fetch(downloadUrl)` streaming pattern was noted in the plan as the correct approach (not `get().stream`), and implemented accordingly.

## Verification Results

- `npx tsc --noEmit` exits 0 after all three tasks
- `npm run build` exits 0; `/api/download` listed as dynamic route (ƒ) alongside `/api/cleanup`, `/api/compress`, `/api/upload`
- `app/api/download/route.ts` has `export const runtime = 'nodejs'` and `export const maxDuration = 30`
- `Content-Disposition: attachment; filename="${safeName}-compressed.pdf"` header present
- `del([compressedBlobUrl, blobUrl])` called after `new Response(stream)` construction (fire-and-forget void)
- `/api/cleanup` accepts `{ url }` and `{ url, compressedUrl }` — both return `{ ok: true }`
- `handleReset` sends `compressedUrl` when `state.compressedBlobUrl` is non-null
- `FileInfoCard` renders error branch only when `stage === 'error'`; `{/* PLAN-2.3: ... */}` comment removed
- Two buttons present: "Повторить" (type=primary, calls onRetry) and "Загрузить другой" (type=default, calls onReset)

## Known Stubs

None — all stubs from PLAN-2.2 resolved:
- `/api/download` route now exists (was missing stub)
- `error` and `onRetry` props in FileInfoCard now fully rendered (were void-suppressed)

## Threat Flags

None — no new network endpoints, auth paths, or schema changes beyond what was planned. `/api/download` validates blobUrl/compressedBlobUrl via Zod `.url()` before use.

## Phase 2 Completion

With PLAN-2.3 complete, Phase 2 delivers the full end-to-end compression flow:
- Upload (Phase 1) → Select preset → Compress (/api/compress) → View stats → Download (/api/download, streams with Content-Disposition) → "Сжать другой файл" resets to idle and cleans up both blobs (/api/cleanup with dual URL)
- Error states handled at both upload (Phase 1 DropZone) and compression (FileInfoCard error stage) layers

## Self-Check: PASSED

- `app/api/download/route.ts` — FOUND
- `app/api/cleanup/route.ts` — FOUND (modified)
- `features/pdf/usePdfUpload.ts` — FOUND (modified)
- `components/FileInfoCard.tsx` — FOUND (modified)
- Commits 027f4db, b4282d2, b30b120 — FOUND in git log

---
*Phase: 02-compression-download*
*Completed: 2026-05-19*
