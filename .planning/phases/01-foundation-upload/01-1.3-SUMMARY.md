---
phase: 01-foundation-upload
plan: "1.3"
subsystem: api-routes
tags: [vercel-blob, api-routes, cleanup, upload-token, zod]
dependency_graph:
  requires: [01-1.1, 01-1.2]
  provides: [upload-token-api, blob-cleanup-api, blobService]
  affects: [phase-2-compress]
tech_stack:
  added: []
  patterns: [handleUpload-token-handshake, deleteBlobSafe-try-catch, zod-body-validation]
key_files:
  created:
    - app/api/upload/route.ts
    - services/blobService.ts
    - app/api/cleanup/route.ts
  modified: []
decisions:
  - "No 'export const runtime' override on upload route — default nodejs handles it (per CLAUDE.md Architecture #3: runtime override only needed on /api/compress)"
  - "onUploadCompleted contains only console.log — no business logic (per RESEARCH.md Pitfall 1: callback silently fails locally)"
  - "BLOB_READ_WRITE_TOKEN stored in process.env only — no NEXT_PUBLIC_ prefix (T-03-03 mitigation)"
  - "INFRA-02 Phase 1 scope: on-reset cleanup via /api/cleanup only; post-download cleanup deferred to Phase 2 /api/download"
metrics:
  duration: "10 minutes"
  completed: "2026-05-18"
---

# Phase 1 Plan 1.3: API Routes (/api/upload + /api/cleanup + Vercel deploy) Summary

**One-liner:** Vercel Blob client-upload token handshake via handleUpload(), safe blob deletion with deleteBlobSafe() wrapper, and zod-validated /api/cleanup endpoint — satisfying INFRA-01 and INFRA-02 (Phase 1 scope).

## Tasks Completed

| Task | Name | Commit | Key Files |
|------|------|--------|-----------|
| 1 | Implement /api/upload token handshake and /api/cleanup endpoint | 0633945 | app/api/upload/route.ts, services/blobService.ts, app/api/cleanup/route.ts |

## Verification Results

- `npx tsc --noEmit` — exits 0, zero TypeScript errors
- `npm run build` — exits 0, both /api/upload and /api/cleanup registered as dynamic routes
- `/api/upload/route.ts` — imports from `@vercel/blob/client`, no `export const runtime`, onBeforeGenerateToken enforces PDF only + 20MB limit
- `/api/cleanup/route.ts` — zod validation, returns 400 on invalid URL, returns `{ok:true}` on success
- `services/blobService.ts` — del() wrapped in try/catch, cleanup failures logged and swallowed

## Deviations from Plan

None — all three files were implemented exactly per plan specification. Build passes, TypeScript clean.

## Known Stubs

None — API routes are fully implemented. End-to-end upload flow requires BLOB_READ_WRITE_TOKEN (human checkpoint Task 2 pending).

## Threat Surface

All T-03 threats from plan's threat model are addressed:

| Threat | File | Mitigation Applied |
|--------|------|--------------------|
| T-03-01: Malicious content-type | app/api/upload/route.ts | allowedContentTypes: ['application/pdf'] in onBeforeGenerateToken |
| T-03-02: File size bypass | app/api/upload/route.ts | maximumSizeInBytes: 20971520 in onBeforeGenerateToken |
| T-03-03: Token leak to browser | app/api/upload/route.ts | handleUpload() called server-side only; no NEXT_PUBLIC_ prefix |
| T-03-05: Forged HandleUploadBody | app/api/upload/route.ts | onBeforeGenerateToken enforces allowedContentTypes server-side |

## Checkpoint Status

**Task 2 (human-verify) is pending.** The checkpoint requires:
1. User creates Vercel Blob store and sets BLOB_READ_WRITE_TOKEN in .env.local
2. User tests full upload flow: browser → /api/upload → Vercel Blob CDN → file visible in Dashboard
3. User deploys to Vercel (INFRA-03)

## Self-Check

### Files exist:
- [x] app/api/upload/route.ts — FOUND
- [x] services/blobService.ts — FOUND
- [x] app/api/cleanup/route.ts — FOUND

### Commits exist:
- [x] 0633945 — FOUND

## Self-Check: PASSED
