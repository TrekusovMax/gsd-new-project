---
phase: 01-foundation-upload
plan: "1.1"
subsystem: scaffold
tags: [next.js, typescript, antd, dark-theme, prettier]
dependency_graph:
  requires: []
  provides: [app-scaffold, type-contracts, dark-theme, eslint-prettier]
  affects: [01-1.2, 01-1.3]
tech_stack:
  added: [next@16.2.6, react@19.2.4, antd@6.4.2, "@ant-design/icons@6.2.3", "@vercel/blob@2.3.3", zod, prettier, eslint-config-prettier]
  patterns: [ConfigProvider+darkAlgorithm, useReducer-state-machine-types, client-component-layout]
key_files:
  created:
    - app/layout.tsx
    - app/globals.css
    - app/page.tsx
    - components/AppHeader.tsx
    - types/upload.ts
    - package.json
    - tsconfig.json
    - next.config.ts
    - eslint.config.mjs
    - .prettierrc
    - .gitignore
    - .env.local.example
  modified: []
decisions:
  - "layout.tsx uses 'use client' to avoid Ant Design SSR hydration mismatch (per RESEARCH.md Pitfall 2)"
  - "next.config.ts adds turbopack.root to silence workspace root detection warning"
  - "ESLint uses flat config (eslint.config.mjs) with eslint-config-prettier spread after nextVitals and nextTs"
metrics:
  duration: "45 minutes"
  completed: "2026-05-18"
---

# Phase 1 Plan 1.1: Next.js Scaffold with Ant Design Dark Theme Summary

**One-liner:** Next.js 16 project initialized with Ant Design 6 dark theme (darkAlgorithm + token overrides), strict TypeScript, Prettier, and all upload state machine type contracts defined.

## Tasks Completed

| Task | Name | Commit | Key Files |
|------|------|--------|-----------|
| 1 | Initialize Next.js project, install deps, configure layout with dark theme, ESLint + Prettier | 90f1153 | package.json, layout.tsx, globals.css, next.config.ts, .prettierrc, eslint.config.mjs |
| 2 | Define TypeScript type contracts and create AppHeader component | 90f1153 | types/upload.ts, components/AppHeader.tsx, app/page.tsx |

## Verification Results

- `npx tsc --noEmit` — exits 0, zero TypeScript errors
- `npm run build` — exits 0, production build succeeds without warnings
- `npx prettier --check "**/*.{ts,tsx}"` — exits 0, all files pass

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Added turbopack.root to next.config.ts**
- **Found during:** Task 1, first build run
- **Issue:** Next.js detected multiple lockfiles (D:\js learn\package-lock.json and project package-lock.json) and warned about workspace root misdetection
- **Fix:** Added `turbopack: { root: path.resolve(__dirname) }` to next.config.ts to explicitly set the workspace root
- **Files modified:** next.config.ts
- **Commit:** 90f1153

**2. [Rule 3 - Blocking] Used pdf-compressor subdirectory then moved files**
- **Found during:** Task 1 scaffolding
- **Issue:** `create-next-app` rejected project name "GSD_Superpowers" due to npm naming restrictions (uppercase letters). Had to create in subdirectory `pdf-compressor`, then copy files to project root.
- **Fix:** Created project in `pdf-compressor/`, copied all generated files to root, installed dependencies separately, removed the temporary directory
- **Files modified:** All scaffold files at project root
- **Commit:** 90f1153

## Known Stubs

- `app/page.tsx` — upload card placeholder div (empty, no content) — intentional; PLAN-1.2 will wire DropZone component here

## Self-Check

### Files exist:
- [x] app/layout.tsx — FOUND
- [x] app/globals.css — FOUND
- [x] app/page.tsx — FOUND
- [x] components/AppHeader.tsx — FOUND
- [x] types/upload.ts — FOUND
- [x] package.json — FOUND
- [x] tsconfig.json — FOUND
- [x] next.config.ts — FOUND
- [x] .prettierrc — FOUND
- [x] .gitignore — FOUND
- [x] .env.local.example — FOUND

### Commits exist:
- [x] 90f1153 — FOUND

## Self-Check: PASSED
